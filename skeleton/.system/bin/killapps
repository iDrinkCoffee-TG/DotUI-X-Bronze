#!/bin/sh

c=0
while [ $c -lt 5 ]; do
	killall telnetd ftpd tcpsvd dropbear picoarch MiniUI
	pgrep telnetd || pgrep ftpd || pgrep tcpsvd || pgrep dropbear || pgrep picoarch || pgrep MiniUI || exit 0
	sleep 0.1
	c=$((c+1))
done
killall -9 telnetd ftpd tcpsvd dropbear picoarch MiniUI
